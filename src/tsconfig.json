/*
 * 这个文件是给 tsc --watch 命令(npm run watch-ts) 使用的
 * tsc 的 watch 如果指定 watch 了文件夹，则指定的文件夹下必须要有 tsconfig.json 配置
 * 这个设定实在是太傻X了
 * 如果不设置 watch 文件夹，则 tsc 编译出来文件到 dist，dist 也会被 watch，然后无限循环编译
 */
{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es6",
    "noImplicitAny": true,
    "moduleResolution": "node",
    "sourceMap": true,
    "outDir": "dist",
    "baseUrl": ".",
    "allowJs": true,
    "suppressImplicitAnyIndexErrors": true,
    "allowSyntheticDefaultImports": true,
    "paths": {
      "*": [
        "../node_modules/*",
        "../src/types/*"
      ]
    },
    "listFiles": true
  },
  "include": [
    "./*"
  ]
}