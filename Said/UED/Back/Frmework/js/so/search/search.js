'use strict';
define(['so'], function (so) {
    //自己的这些插件一定要依赖so插件，定义模板引擎等
    var globalOption = {
        templent: ''
        //, def: '没有检索到相关信息'
    };
    search = function (elem, data, option) {

    };
    return function (data) {

    };
});